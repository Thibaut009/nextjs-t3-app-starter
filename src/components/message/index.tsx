import styles from "~/components/message/styles.module.css"

export default function Message() {
  return (
    <section className={styles.section}>
      <h2 className={styles.h2}>Message component</h2>
    </section>
  );
}
